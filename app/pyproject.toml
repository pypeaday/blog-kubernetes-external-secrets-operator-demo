[project]
name = "demo-app"
version = "0.1.0"
description = "External Secrets Operator Demo App"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.100.0",
    "uvicorn[standard]>=0.23.0",
]

[project.scripts]
demo-app = "main:app"

[tool.uv]
dev-dependencies = []
